{% set top_enabled = true %}

{# Logo #}
{% set light_logo_url = theme.settings.get('veloura_light_logo_url_2026') %}
{% set light_logo_image = theme.settings.get('veloura_light_logo_2026') %}
{% set veloura_light_logo = light_logo_url ?: light_logo_image %}

{% set top_logo_url = theme.settings.get('veloura_top_logo_url_2026') %}
{% set top_logo_image = theme.settings.get('veloura_top_logo_image_2026') %}
{% set top_logo = top_logo_url ?: top_logo_image ?: veloura_light_logo ?: store.logo|cdn(175) %}

{% set top_logo_lower = top_logo|lower %}
{% set top_logo_is_video = top_logo_lower ends with '.webm' or top_logo_lower ends with '.mp4' %}

{# General Settings #}
{% set top_logo_center = theme.settings.get('veloura_top_logo_center_2026', true) %}
{% set top_logo_no_center_laptop = theme.settings.get('veloura_top_logo_no_center_laptop_2026', false) %}
{% set top_logo_size_value = theme.settings.get('veloura_top_logo_size_2026', 5) %}
{% set top_logo_size_px = 32 + (top_logo_size_value * 3) %}

{% set top_bg = theme.settings.get('veloura_top_bg_color_2026', '#ffffff') %}
{% set top_text = theme.settings.get('veloura_top_text_color_2026', '#004d65') %}
{% set top_cart_bg = theme.settings.get('veloura_top_cart_bg_2026', '#f87171') %}

{% set top_border = theme.settings.get('veloura_top_border_style_2026', 'sharp') %}
{% set icons_size = theme.settings.get('veloura_top_icons_size_2026', 'small') %}

{% set mobile_search = theme.settings.get('veloura_top_mobile_search_display_2026', 'icon_right') %}
{% set desktop_search = theme.settings.get('veloura_top_desktop_search_display_2026', 'bar_sticky_icon') %}
{% set desktop_menu_mode = theme.settings.get('veloura_top_desktop_menu_mode_2026', 'links') %}

{% set show_mobile_lang = theme.settings.get('veloura_top_mobile_currency_lang_icon_2026', true) %}
{% set hide_lang_desktop = theme.settings.get('veloura_top_hide_currency_lang_desktop_2026', false) %}
{% set hide_cart_total_mobile = theme.settings.get('veloura_top_hide_cart_total_mobile_2026', true) %}

{% set full_width_desktop = theme.settings.get('veloura_top_full_width_desktop_2026', true) %}
{% set show_wishlist = theme.settings.get('veloura_top_show_wishlist_2026', false) %}

{% set dark_pos = theme.settings.get('veloura_dark_toggle_position_2026', 'left') %}

<header
  class="store-header
  veloura-top-enabled
  {{ top_logo_center ? 'veloura-top-logo-centered' : '' }}
  {{ top_logo_no_center_laptop ? 'veloura-top-logo-no-center-laptop' : '' }}
  {{ full_width_desktop ? 'veloura-top-full-width-desktop' : '' }}
  {{ hide_lang_desktop ? 'veloura-top-hide-lang-desktop' : '' }}
  {{ theme.settings.get('veloura_top_hide_on_scroll_2026') ? 'veloura-hide-top-on-scroll' : '' }}

  veloura-top-border-{{ top_border }}
  veloura-top-icons-{{ icons_size }}

  veloura-mobile-search-{{ mobile_search }}
  veloura-desktop-search-{{ desktop_search }}
  veloura-desktop-menu-{{ desktop_menu_mode }}
  "
  style="
    --veloura-top-bg: {{ top_bg }};
    --veloura-top-text: {{ top_text }};
    --veloura-top-cart-bg: {{ top_cart_bg }};
    --veloura-top-logo-size: {{ top_logo_size_px }}px;
  "
>
  <div id="mainnav" class="main-nav-container shadow-default bg-white">
    <div class="inner bg-inherit w-full">
      <div class="veloura-header-container">
        <div class="veloura-header-grid">

          {# Left Side #}
          <div class="veloura-header-side veloura-header-left">

            {% if not hide_lang_desktop %}
              <div class="veloura-lang-desktop">
                {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
                  <salla-localization-modal show-trigger></salla-localization-modal>
                {% endif %}
              </div>
            {% endif %}

            <salla-cart-summary class="veloura-cart-summary {{ hide_cart_total_mobile ? 'veloura-cart-hide-total veloura-cart-icon-only-mobile' : '' }}">
  <span slot="icon" class="veloura-cart-custom-icon">
    <i class="{{ theme.settings.get('veloura_top_cart_icon_2026', 'sicon-shopping-bag') }}"></i>
  </span>
</salla-cart-summary>

            <button type="button" class="veloura-header-icon veloura-login-btn" onclick="salla.event.dispatch('login::open')" aria-label="تسجيل الدخول">
              <i class="{{ theme.settings.get('veloura_top_login_icon_2026', 'sicon-user-circle') }}"></i>
            </button>

            {% if show_wishlist %}
              <a class="veloura-header-icon veloura-wishlist-btn" href="{{ store.url }}/wishlist" aria-label="المفضلة">
                <i class="sicon-heart"></i>
              </a>
            {% endif %}

            {% if theme.settings.get('veloura_dark_enabled_2026') and theme.settings.get('veloura_dark_toggle_enabled_2026') and dark_pos == 'left' %}
              <button type="button" class="veloura-header-icon veloura-dark-toggle" aria-label="تبديل الوضع الداكن">
                <i class="sicon-moon veloura-dark-toggle__icon"></i>
              </button>
            {% endif %}

            <button
              type="button"
              class="veloura-header-icon veloura-search-toggle veloura-search-toggle-left"
              onclick="salla.event.dispatch('search::open')"
              aria-label="بحث">
              <i class="sicon-search"></i>
            </button>

          </div>

          {# Logo #}
          <a class="navbar-brand veloura-header-logo" href="{{ store.url }}">
            {% if top_logo_is_video %}
              <video
                class="veloura-store-logo veloura-store-logo-video"
                src="{{ top_logo }}"
                autoplay
                muted
                loop
                playsinline
                data-light-logo="{{ top_logo }}"
                data-dark-logo="{{ theme.settings.get('veloura_dark_logo_2026') }}"
                aria-label="{{ store.name }} logo">
              </video>
            {% else %}
              <img
                fetchpriority="high"
                width="100%"
                height="100%"
                loading="eager"
                src="{{ top_logo }}"
                data-light-logo="{{ top_logo }}"
                data-dark-logo="{{ theme.settings.get('veloura_dark_logo_2026') }}"
                class="veloura-store-logo"
                alt="{{ store.name }} logo">
            {% endif %}

            {% if is_page('index') %}
              <h1 class="sr-only">{{ store.name }}</h1>
            {% else %}
              <h2 class="sr-only">{{ store.name }}</h2>
            {% endif %}
          </a>

          {# Right Side #}
          <div class="veloura-header-side veloura-header-right">

            <button
              type="button"
              class="veloura-header-icon veloura-search-toggle veloura-search-toggle-right"
              onclick="salla.event.dispatch('search::open')"
              aria-label="بحث">
              <i class="sicon-search"></i>
            </button>

            {% if theme.settings.get('veloura_dark_enabled_2026') and theme.settings.get('veloura_dark_toggle_enabled_2026') and dark_pos == 'right' %}
              <button type="button" class="veloura-header-icon veloura-dark-toggle" aria-label="تبديل الوضع الداكن">
                <i class="sicon-moon veloura-dark-toggle__icon"></i>
              </button>
            {% endif %}

            {% if show_mobile_lang %}
              <button type="button" class="veloura-header-icon veloura-lang-mobile" onclick="document.querySelector('salla-localization-modal')?.click()" aria-label="اللغة والعملة">
                <i class="sicon-earth"></i>
              </button>
            {% endif %}

            <div class="veloura-cats-sheet__body">
  <div class="veloura-cats-sheet__list" data-veloura-cats-list>
    <div class="veloura-cats-sheet__loading">جاري تحميل التصنيفات...</div>
  </div>
</div>

            <a class="veloura-header-icon veloura-menu-trigger-mobile mburger mburger--collapse" href="#mobile-menu" aria-label="Open-menu">
  <i class="{{ theme.settings.get('veloura_top_menu_icon_2026', 'sicon-menu') }}"></i>
</a>

<button type="button" class="veloura-header-icon veloura-menu-trigger-desktop" aria-label="فتح القائمة">
  <i class="{{ theme.settings.get('veloura_top_menu_icon_2026', 'sicon-menu') }}"></i>
</button>

          </div>

          {# Search Bars #}
          <div class="veloura-mobile-search-bar">
            <salla-search inline oval height="40"></salla-search>
          </div>

          <div class="veloura-desktop-search-bar">
            <salla-search inline oval height="40"></salla-search>
          </div>

        </div>
      </div>
    </div>
  </div>


  <div class="veloura-desktop-menu-overlay"></div>

<div class="veloura-desktop-menu-drawer" aria-hidden="true">
  <div class="veloura-desktop-menu-drawer__head">
    <span>القائمة</span>

    <button type="button" class="veloura-desktop-menu-close" aria-label="إغلاق القائمة">
      <i class="sicon-cancel"></i>
    </button>
  </div>

  <div class="veloura-desktop-menu-drawer__body">
    <custom-main-menu></custom-main-menu>
  </div>
</div>

</header>
{# ================================
   Veloura Categories Bottom Sheet
   ضع هذا بعد </header> مباشرة
================================ #}

<div
  class="veloura-cats-sheet"
  id="veloura-cats-sheet"
  aria-hidden="true"
  hidden
  style="display:none"
>
  <div class="veloura-cats-sheet__overlay" data-veloura-cats-close></div>

  <div class="veloura-cats-sheet__panel" role="dialog" aria-modal="true" aria-label="التصنيفات">
    <button type="button" class="veloura-cats-sheet__close" data-veloura-cats-close aria-label="إغلاق التصنيفات"></button>

    <div class="veloura-cats-sheet__handle"></div>

    <div class="veloura-cats-sheet__head">
      <strong>التصنيفات</strong>
    </div>

    <div class="veloura-cats-sheet__body">
      <div class="veloura-cats-sheet__list" data-veloura-cats-list>
        <div class="veloura-cats-sheet__loading">جاري تحميل التصنيفات...</div>
      </div>
    </div>
  </div>
</div>

<style>
  .veloura-cats-sheet {
    display: none !important;
  }

  .veloura-cats-sheet,
  .veloura-cats-sheet * {
    box-sizing: border-box;
  }

  @media (min-width: 768px) {
    .veloura-cats-sheet {
      display: none !important;
      visibility: hidden !important;
      opacity: 0 !important;
      pointer-events: none !important;
    }
  }

  @media (max-width: 767px) {
    body.veloura-mobile-floating-menu-enabled .veloura-mobile-floating-menu {
      z-index: 999999 !important;
    }

    body.veloura-cats-sheet-open {
      overflow: hidden !important;
    }

    body.veloura-cats-sheet-open .veloura-cats-sheet {
      display: block !important;
      visibility: visible !important;
      opacity: 1 !important;
      pointer-events: auto !important;
    }

    .veloura-cats-sheet {
      position: fixed !important;
      inset: 0 !important;
      z-index: 999970 !important;
      width: 100vw !important;
      height: 100vh !important;
      overflow: hidden !important;
    }

    .veloura-cats-sheet__overlay {
      position: absolute !important;
      inset: 0 !important;
      background: rgba(15, 23, 42, 0.42) !important;
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
    }

    .veloura-cats-sheet__panel {
      position: absolute !important;
      left: 12px !important;
      right: 12px !important;
      width: auto !important;

      bottom: calc(
        var(--veloura-mobile-floating-menu-height, 56px) +
        var(--veloura-mobile-floating-menu-bottom, 16px) +
        env(safe-area-inset-bottom, 0px) +
        14px
      ) !important;

      max-height: min(62vh, 560px) !important;
      overflow: hidden !important;

      border-radius: 28px !important;
      background: #ffffff !important;
      color: #111827 !important;

      box-shadow:
        0 -18px 55px rgba(15, 23, 42, 0.24),
        inset 0 0 0 1px rgba(15, 23, 42, 0.06) !important;

      transform: translate3d(0, 110%, 0);
      opacity: 0;

      transition:
        transform 0.28s cubic-bezier(.2, .8, .2, 1),
        opacity 0.22s ease;
    }

    body.veloura-cats-sheet-open .veloura-cats-sheet__panel {
      transform: translate3d(0, 0, 0) !important;
      opacity: 1 !important;
    }

    .veloura-cats-sheet__handle {
      width: 44px;
      height: 5px;
      margin: 12px auto 8px;
      border-radius: 999px;
      background: rgba(15, 23, 42, 0.16);
    }

    .veloura-cats-sheet__close {
      position: absolute !important;
      top: 10px !important;
      left: 12px !important;
      z-index: 5 !important;

      width: 34px !important;
      height: 34px !important;

      border: 0 !important;
      border-radius: 999px !important;

      background: rgba(15, 23, 42, 0.08) !important;
      color: #111827 !important;

      display: inline-flex !important;
      align-items: center !important;
      justify-content: center !important;

      cursor: pointer !important;
    }

    .veloura-cats-sheet__close::before {
      content: '×';
      font-size: 25px;
      line-height: 1;
      transform: translateY(-1px);
    }

    .veloura-cats-sheet__head {
      padding: 0 18px 12px;
      text-align: center;
      font-size: 16px;
      font-weight: 900;
      border-bottom: 1px solid rgba(15, 23, 42, 0.08);
    }

    .veloura-cats-sheet__body {
      max-height: calc(min(62vh, 560px) - 72px);
      overflow-y: auto;
      overflow-x: hidden;
      -webkit-overflow-scrolling: touch;
      padding: 4px 0 10px;
    }

    .veloura-cats-sheet__loading,
    .veloura-cats-sheet__empty {
      padding: 24px 18px;
      text-align: center;
      color: rgba(15, 23, 42, 0.55);
      font-size: 13px;
      font-weight: 700;
    }

    .veloura-cats-sheet__link {
      min-height: 54px;
      padding: 0 18px;

      display: flex !important;
      align-items: center !important;
      justify-content: space-between !important;
      gap: 12px;

      color: #111827 !important;
      text-decoration: none !important;

      border-bottom: 1px solid rgba(15, 23, 42, 0.07);
      font-size: 14px;
      font-weight: 800;
    }

    .veloura-cats-sheet__link:hover {
      background: rgba(15, 23, 42, 0.04);
    }

    .veloura-cats-sheet__link i {
      font-size: 16px;
      opacity: 0.45;
    }

    body.veloura-cats-sheet-open
      .veloura-mobile-floating-menu
      [data-vmfm-action="categories"]
      i,
    body.veloura-cats-sheet-open
      .veloura-mobile-floating-menu
      [data-vmfm-action="categories"]
      span {
      opacity: 0 !important;
      visibility: hidden !important;
    }
  }
</style>

<script data-cfasync="false">
  document.addEventListener('DOMContentLoaded', function () {
    var sheet = document.getElementById('veloura-cats-sheet');
    var list = sheet ? sheet.querySelector('[data-veloura-cats-list]') : null;

    if (!sheet || !list) {
      return;
    }

    function cleanText(value) {
      return String(value || '').replace(/\s+/g, ' ').trim();
    }

    function escapeHtml(value) {
      return String(value || '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#039;');
    }

    function hideSheetHard() {
      sheet.hidden = true;
      sheet.style.display = 'none';
      sheet.setAttribute('aria-hidden', 'true');
    }

    function showSheetHard() {
      sheet.hidden = false;
      sheet.style.display = 'block';
      sheet.setAttribute('aria-hidden', 'false');
    }

    function isValidMenuLink(link) {
      if (!link) {
        return false;
      }

      var text = cleanText(link.textContent);
      var href = link.getAttribute('href') || '';

      if (!text) {
        return false;
      }

      if (href === '#mobile-menu') {
        return false;
      }

      if (link.closest('.veloura-mobile-floating-menu')) {
        return false;
      }

      if (link.closest('.veloura-cats-sheet')) {
        return false;
      }

      if (link.closest('.veloura-header-logo')) {
        return false;
      }

      return true;
    }

    function getMenuLinks() {
      var selectors = [
        '.veloura-menu-links-wrap a',
        '.veloura-desktop-menu-drawer a',
        'custom-main-menu a',
        '#mobile-menu a',
        '.main-menu a',
        'a[href*="/categories"]',
        'a[href*="/category"]',
        'a[href*="/c/"]'
      ];

      var links = [];
      var seen = {};

      selectors.forEach(function (selector) {
        document.querySelectorAll(selector).forEach(function (link) {
          if (!isValidMenuLink(link)) {
            return;
          }

          var text = cleanText(link.textContent);
          var href = link.getAttribute('href') || '#';
          var key = text + '|' + href;

          if (seen[key]) {
            return;
          }

          seen[key] = true;

          links.push({
            text: text,
            href: href
          });
        });
      });

      return links;
    }

    function renderCatsSheet() {
      var links = getMenuLinks();

      if (!links.length) {
        list.innerHTML = '<div class="veloura-cats-sheet__loading">جاري تحميل التصنيفات...</div>';
        return false;
      }

      list.innerHTML = links.map(function (item) {
        return (
          '<a class="veloura-cats-sheet__link" href="' + escapeHtml(item.href) + '">' +
            '<span>' + escapeHtml(item.text) + '</span>' +
            '<i class="sicon-keyboard_arrow_left" aria-hidden="true"></i>' +
          '</a>'
        );
      }).join('');

      return true;
    }

    function keepTryingRender() {
      if (renderCatsSheet()) {
        return;
      }

      setTimeout(renderCatsSheet, 300);
      setTimeout(renderCatsSheet, 700);
      setTimeout(renderCatsSheet, 1200);
      setTimeout(renderCatsSheet, 2000);
      setTimeout(renderCatsSheet, 3500);
    }

    function openCatsSheet() {
      if (window.innerWidth > 767) {
        return;
      }

      keepTryingRender();
      showSheetHard();

      requestAnimationFrame(function () {
        document.body.classList.add('veloura-cats-sheet-open');
      });
    }

    function closeCatsSheet() {
      document.body.classList.remove('veloura-cats-sheet-open');

      setTimeout(function () {
        hideSheetHard();
      }, 240);
    }

    window.velouraOpenCatsSheet = openCatsSheet;
    window.velouraOpenCategoriesBottomSheet = openCatsSheet;

    hideSheetHard();
    keepTryingRender();

    var observer = new MutationObserver(function () {
      if (!document.body.classList.contains('veloura-cats-sheet-open')) {
        renderCatsSheet();
      }
    });

    observer.observe(document.body, {
      childList: true,
      subtree: true
    });

    document.addEventListener(
      'click',
      function (event) {
        var floatingCats = event.target.closest(
          '.veloura-mobile-floating-menu [data-vmfm-action="categories"]'
        );

        if (floatingCats) {
          event.preventDefault();
          event.stopPropagation();
          openCatsSheet();
          return;
        }

        var close = event.target.closest('[data-veloura-cats-close]');

        if (close) {
          event.preventDefault();
          closeCatsSheet();
          return;
        }

        var sheetLink = event.target.closest('.veloura-cats-sheet__link');

        if (sheetLink) {
          closeCatsSheet();
        }
      },
      true
    );

    window.addEventListener('resize', function () {
      if (window.innerWidth > 767) {
        closeCatsSheet();
        hideSheetHard();
      }
    });

    document.addEventListener('keydown', function (event) {
      if (event.key === 'Escape') {
        closeCatsSheet();
      }
    });
  });
</script>